# 2026-08-23：Cloudflare Pages 原生 SPA 支持（项目无顶层 404.html 时，
# 未知路径自动匹配根 /，返回 200 index.html）。
# 此前 /* /index.html 200 会与 auto pretty-URL（/index.html → / 的 308）冲突而失效，
# 并触发 "Infinite loop" 构建警告——已移除，勿恢复。
